global:
  resolve_timeout: 5m

route:
  receiver: 'webhook-receiver'

receivers:
  - name: 'webhook-receiver'
    webhook_configs:
      - url: 'http://ansible-webhook:5001/'   # docker-compose service name + port
        send_resolved: true
        